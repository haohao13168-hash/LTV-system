{
  "name": "bcb-sync",
  "version": "1.0.0",
  "private": true,
  "description": "Pulls BCB wallet API data and writes snapshots to Supabase. Runs on the DigitalOcean droplet so we get a fixed IP for the BCB whitelist.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "sync-once": "node sync-once.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=20"
  }
}
